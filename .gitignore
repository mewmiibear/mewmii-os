.env
/config.php

# User-uploaded content (product/variation images, written at runtime by
# includes/image_upload.php). Never tracked, never touched by a git-based clean/reset - see
# DEPLOYMENT.md for why this directory must also be explicitly preserved by whatever deploys
# this repository to the server.
/uploads/

/vendor

/node_modules

/storage/logs

/storage/temp

.DS_Store

Thumbs.db